import styled from 'styled-components';

export const CardContainer = styled.main`
  background: #161a2b;
  max-width: 500px;
  margin: 3rem auto 0;
  border-radius: 5px;
  color: #fff;
  padding: 20px 25px;

  h2 { 
    margin-bottom: 10px;
  }

  h2, h4 {
    text-align: center;
  }
 
`